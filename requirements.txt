# Requires Python >=3.12, which the numpy pin below enforces (every numpy 2.5.x
# declares Requires-Python >=3.12). The README says 3.12; CI tests 3.12 only.
# Keep those three in step -- they disagreed once, and the resulting CI job
# failed at dependency install on every commit without ever running a test.
#
# Pinned with compatible-release bounds (~=) so a patch upgrade is allowed but
# a minor/major bump cannot silently change numerics. The README claims
# deterministic, reproducible offline execution; unbounded ">=" pins made that
# claim false. Versions are those this project is actually tested against.
numpy~=2.5.1
scipy~=1.17.1
torch~=2.11.0
fastapi~=0.139.0
uvicorn[standard]~=0.34.0
pydantic~=2.11.0
yfinance~=1.5.1
httpx~=0.28.1
groq~=0.9.0
websockets~=15.0
python-dotenv~=1.0.1
